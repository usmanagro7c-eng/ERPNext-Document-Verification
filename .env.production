# Production (build default mode) — verification always runs against the
# Cloudflare Worker proxy, which talks to ERPNext. No mock anywhere.
# Build-time only: VITE_* is inlined at build; local `.env` (dev) is untouched.